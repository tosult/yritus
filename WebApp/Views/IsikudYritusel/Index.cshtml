@model IEnumerable<Domain.App.IsikYritusel>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Yritus)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Isik)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.JurIsik)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Yritus.Asukoht)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Isik.Eesnimi)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.JurIsik.Nimi)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
